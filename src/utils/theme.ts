import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    primary: {
      main: "#6bb3c7",
      light: "#C94F44",
      dark: "#f2f2f2",
    },
    secondary: {
      main: "#010010",
      light: "#ffffff",
      dark: "#cccccc",
    },
  },
});

export default theme;
